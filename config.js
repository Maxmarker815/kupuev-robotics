/* =====================================================================
   ОБЩИЕ НАСТРОЙКИ — одно место на всю платформу.
   Этот файл подключают и index.html (кабинеты), и course.html (уроки),
   поэтому менять адрес таблицы нужно только здесь.
   ===================================================================== */
const CONFIG = {
  // Один и тот же файл живёт в двух местах, и платформа сама понимает, где она:
  // - GitHub Pages (github.io) и файл, открытый с диска — ДЕМО на вымышленных
  //   данных прямо в браузере, без сервера;
  // - хостинг школы — настоящая база через api.php (папка backend/ рядом
  //   с проектом, см. НАСТРОЙКА.md). Адрес относительный: api.php лежит рядом
  //   с index.html, поэтому работает и с временного адреса Hostinger,
  //   и с robotics.kupuev.academy — ничего руками не вписывать.
  API_URL: (location.protocol === 'file:' || /\.github\.io$/.test(location.hostname)) ? '' : 'api.php',
  GUIDE_URL: 'course.html',
  SCHOOL_NAME: 'KUPUEV ACADEMY'
};
